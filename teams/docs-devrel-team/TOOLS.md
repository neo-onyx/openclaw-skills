# TOOLS.md - Documentation / DevRel Team

Skills work with OpenAPI URLs/paths and repo paths. Optional: doc site generator (e.g. Docusaurus) path in .env.

## Output Conventions

- **Project:** From API name or repo. Paths: `output/docs/{project}/discovery/`, `reference/`, `examples/`, `publish/`.

## Pipeline

api_explorer → doc_writer → example_tutorial_lead → publisher
